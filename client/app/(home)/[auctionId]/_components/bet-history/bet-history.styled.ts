'use client';

import styled from 'styled-components';

import { colors } from '@/lib/colors';

export const Header = styled.h2`
    font-weight: 700;
    font-size: 60px;
    line-height: 120%;
    text-align: center;
    color: ${colors.black}
}`;
